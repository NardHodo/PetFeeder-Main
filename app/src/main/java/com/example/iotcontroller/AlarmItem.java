/*
package com.example.iotcontroller;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.TextView;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.appcompat.widget.SwitchCompat;
import androidx.cardview.widget.CardView;

public class AlarmItem extends CardView{

    private TextView assignedTime;
    private TextView assignedDay;
    private SwitchCompat alarmSwitch;

    public AlarmItem(@NonNull Context context){
        super(context);
        innitialize(context);
    }

    public AlarmItem(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
        innitialize(context);
    }

    public void innitialize(Context context){
        inflate(context, R.layout.activity_schedule_view, this);
        assignedTime = findViewById(R.id.assignedTme);
        assignedDay = findViewById(R.id.assignedDay);
        alarmSwitch = findViewById(R.id.alarmSwitch);

    }

    public String setItemId(){

    }
}

 */
